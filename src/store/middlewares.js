/**
 * @author walid
 * @description 中间层增加log日志
 */

// import createLogger from 'vuex/logger'

// export default process.env.NODE_ENV !== 'production' ? [createLogger()] : []

export default process.env.NODE_ENV !== 'production' ? [] : []
